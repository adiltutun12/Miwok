<?xml version="1.0" encoding="utf-8"?>

<!-- Ne radi ovo ovako mora se u manifest dodati MainActivity da bi se aplikacija uopšte mogla pokrenuti
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miwok"
        tools:targetApi="31" />

</manifest>
-->

<!-- Nezakomenatrisana verzija je incijalna koja nema definisanu početnu aktivnost, pa apk ne zna odakle da počne zapravo
Intent filter oznacava androidu koja je početna aktivnost sa kojom treba započeti aplikaciju-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
package="com.example.miwok">

<application
    android:name=".MiwokApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.MiwokApplicationKotlin"
    tools:targetApi="31">
    <!--Imao sam ovdje problema nije mi se htjela aplikacija, samo se instalira ali ne i pokrne da bih je mogao koristiti
    zato sam uradio ovo sa theme da bi se moglo učitati-->

    <activity
        android:name=".MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

</application>

</manifest>
